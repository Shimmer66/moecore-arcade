# 跑酷开发素材

来自用户提供的 `萌芯游乐园_AI娘跑酷_400张_一图一素材.zip`：
400 张独立 RGBA PNG，约 21 MB。保留素材清单、包内质量记录和校验值，不导入离线预览页面。
`QA_REPORT.json` 是原包声明；项目测试另行验证文件数量、PNG 头、尺寸和 SHA-256。

| 类别             | 数量 |
| ---------------- | ---: |
| 四位角色静态姿态 |  168 |
| 头像与表情       |   26 |
| 地块与场景       |   40 |
| 障碍与敌人       |   40 |
| 道具与收集物     |   40 |
| 静态特效         |   48 |
| 界面素材         |   38 |

原包描述为 AI 生成参考图的切分结果，不是网友原作复制，也未提供第三方授权证明。
状态保留为 `generated-pending-review`。当前只在本地开发中预览，正式构建使用自制几何图形。
素材登记不是公开发布授权确认。

姿态不是对齐后的逐帧动画，地块不保证无缝拼接；界面采用状态贴图和独立逻辑碰撞体。
通过 `@moecore/assets/parkour` 的 `loadParkourAssets(ids)` 只解析需要的素材 URL，不一次下载 400 张图片。
