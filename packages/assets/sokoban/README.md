# 大肥鱼推箱子素材

这里存放推箱子游戏的运行时素材。`manifest.json` 与 `CHECKSUMS.sha256` 为当前包的索引和校验记录。

素材来自本次共创项目的生成图，当前标记为 `generated-pending-review`，仅用于原型试玩；正式发布前需要完成版权、角色一致性和透明边缘检查。游戏代码通过 `@moecore/assets/sokoban` 按 manifest 加载资源，不直接拼接文件路径。
